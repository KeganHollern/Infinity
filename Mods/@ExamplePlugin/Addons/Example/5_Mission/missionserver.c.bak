modded class MissionServer
{
	override void OnInit()
	{
		Print("RUNNING MOD!");
		ExampleTestClass test = ExampleTestClass.Create("Test!");
		string name = test.GetName();
		Print(name);
		Print(test);
		super.OnInit();
	}
}